import React, { useState, useEffect } from 'react';
import styles from './styles.module.sass';

const Header = ({ typePosition }: any) => {

    return (
        <header>
    
           
        </header>
    );
};

export default Header;
